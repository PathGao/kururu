name: Release

# Owner-triggered releases must name a verified commit on main before the
# protected signing environment can be approved.
on:
  push:
    tags: ["v[0-9]*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Version tag matching Resources/Info.plist (for example v0.1.2)
        required: true
        type: string

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

permissions: {}

concurrency:
  group: release-signing
  cancel-in-progress: false

jobs:
  preflight:
    name: Verify trusted release ref
    if: >-
      github.repository == 'PathGao/kururu' &&
      ((github.event_name == 'push' && github.ref_type == 'tag') ||
       (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) &&
      github.actor == 'PathGao' &&
      github.triggering_actor == 'PathGao'
    runs-on: macos-26
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Verify release source
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-(beta|rc|alpha)\.[0-9]+)?$ ]]; then
            echo "Unsupported release tag: $RELEASE_TAG" >&2
            exit 1
          fi

          COMMIT_SHA="$(git rev-parse 'HEAD^{commit}')"
          VERIFIED="$(gh api "repos/$GITHUB_REPOSITORY/commits/$COMMIT_SHA" --jq '.commit.verification.verified')"
          if [[ "$VERIFIED" != "true" ]]; then
            echo "Release commit is not verified by GitHub" >&2
            exit 1
          fi

          RELATION="$(gh api "repos/$GITHUB_REPOSITORY/compare/$COMMIT_SHA...main" --jq '.status + ":" + .merge_base_commit.sha')"
          if [[ "$RELATION" != "identical:$COMMIT_SHA" && "$RELATION" != "ahead:$COMMIT_SHA" ]]; then
            echo "Release tag does not point to a commit on main" >&2
            exit 1
          fi

          VERSION="${RELEASE_TAG#v}"
          APP_VERSION="$(/usr/libexec/PlistBuddy -c 'Print CFBundleShortVersionString' Resources/Info.plist)"
          if [[ "$APP_VERSION" != "$VERSION" ]]; then
            echo "Tag $RELEASE_TAG does not match app version $APP_VERSION" >&2
            exit 1
          fi
          if ! grep -Eq "^## \\[$VERSION\\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$" CHANGELOG.md; then
            echo "CHANGELOG.md has no dated release entry for $VERSION" >&2
            exit 1
          fi

      - name: Unit tests
        run: ./build.sh --test

  release:
    name: Build, sign & notarize DMG
    needs: preflight
    if: >-
      needs.preflight.result == 'success' &&
      github.repository == 'PathGao/kururu' &&
      ((github.event_name == 'push' && github.ref_type == 'tag') ||
       (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) &&
      github.actor == 'PathGao' &&
      github.triggering_actor == 'PathGao'
    runs-on: macos-26
    timeout-minutes: 30
    environment: release-signing
    env:
      APPLE_SIGNING_IDENTITY: ${{ vars.APPLE_SIGNING_IDENTITY }}
      REQUIRE_SIGNING: "1"
    outputs:
      commit_sha: ${{ steps.source.outputs.commit_sha }}
      prerelease: ${{ steps.notes.outputs.prerelease }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Reverify protected release ref
        id: source
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          COMMIT_SHA="$(git rev-parse 'HEAD^{commit}')"
          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            REMOTE_COMMIT="$(gh api "repos/$GITHUB_REPOSITORY/commits/$RELEASE_TAG" --jq '.sha')"
            if [[ "$REMOTE_COMMIT" != "$COMMIT_SHA" ]]; then
              echo "Release tag moved after preflight" >&2
              exit 1
            fi
          fi
          RELATION="$(gh api "repos/$GITHUB_REPOSITORY/compare/$COMMIT_SHA...main" --jq '.status + ":" + .merge_base_commit.sha')"
          if [[ "$RELATION" != "identical:$COMMIT_SHA" && "$RELATION" != "ahead:$COMMIT_SHA" ]]; then
            echo "Release tag no longer points to a commit on main" >&2
            exit 1
          fi
          echo "commit_sha=$COMMIT_SHA" >> "$GITHUB_OUTPUT"

      - name: Set up signing
        env:
          REQUIRE_SIGNING: "1"
          SIGNING_CERT_P12: ${{ secrets.SIGNING_CERT_P12 }}
          SIGNING_CERT_PASSWORD: ${{ secrets.SIGNING_CERT_PASSWORD }}
        run: |
          test -n "$APPLE_SIGNING_IDENTITY"
          ./Tools/ci-setup-signing.sh

      - name: Build
        run: ./build.sh

      - name: Verify release signature
        run: |
          REQUIREMENT='identifier "com.pathgao.kururu" and anchor apple generic and certificate leaf[subject.OU] = "GN56VLVTJ6"'
          codesign --verify --deep --strict -R="$REQUIREMENT" build/stage/kururu.app

      - name: Selftest
        run: ./build/kururu --selftest

      - name: Notarize app
        env:
          REQUIRE_NOTARIZATION: "1"
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
          NOTARY_API_KEY_P8: ${{ secrets.NOTARY_API_KEY_P8 }}
          NOTARY_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.NOTARY_ISSUER_ID }}
        run: ./Tools/notarize.sh build/stage/kururu.app

      - name: Package DMG
        run: ./Tools/make-dmg.sh

      - name: Sign DMG
        run: |
          DMG=$(ls dist/*.dmg | head -1)
          codesign --force --timestamp \
            --sign "$APPLE_SIGNING_IDENTITY" "$DMG"
          REQUIREMENT='anchor apple generic and certificate leaf[subject.OU] = "GN56VLVTJ6"'
          codesign --verify --strict -R="$REQUIREMENT" "$DMG"

      - name: Notarize DMG
        env:
          REQUIRE_NOTARIZATION: "1"
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
          NOTARY_API_KEY_P8: ${{ secrets.NOTARY_API_KEY_P8 }}
          NOTARY_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.NOTARY_ISSUER_ID }}
        run: ./Tools/notarize.sh dist/*.dmg

      - name: Verify notarization
        run: |
          DMG=$(ls dist/*.dmg | head -1)
          DMG_REQUIREMENT='anchor apple generic and certificate leaf[subject.OU] = "GN56VLVTJ6"'
          codesign --verify --strict -R="$DMG_REQUIREMENT" "$DMG"
          xcrun stapler validate "$DMG"
          MP=$(mktemp -d)
          cleanup() {
            hdiutil detach "$MP" >/dev/null 2>&1 || true
            rmdir "$MP" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          hdiutil attach -nobrowse -readonly -mountpoint "$MP" "$DMG"
          spctl -a -t exec -vv "$MP/kururu.app" 2>&1 | tee /tmp/spctl.txt
          grep -q "source=Notarized Developer ID" /tmp/spctl.txt
          REQUIREMENT='identifier "com.pathgao.kururu" and anchor apple generic and certificate leaf[subject.OU] = "GN56VLVTJ6"'
          codesign --verify --deep --strict -R="$REQUIREMENT" "$MP/kururu.app"
          xcrun stapler validate "$MP/kururu.app"

      - name: Release notes from CHANGELOG
        id: notes
        run: |
          VERSION="${RELEASE_TAG#v}"
          NOTES="$(awk -v hdr="## [$VERSION]" 'index($0,hdr)==1{g=1;next} /^## \[/&&g{exit} g{print}' CHANGELOG.md)"
          if [[ -z "${NOTES//[[:space:]]/}" ]]; then
            echo "CHANGELOG.md entry for $VERSION is empty" >&2
            exit 1
          fi
          NOTES_FILE="$RUNNER_TEMP/release-notes.md"
          {
            printf '%s\n' "$NOTES"
            echo ""
            echo "Signed with an Apple Developer ID and notarized by Apple, so it downloads and opens normally. Requires macOS 14 or later. Open the .dmg below and drag kururu to Applications."
          } > "$NOTES_FILE"
          {
            echo "file=$NOTES_FILE"
            if [[ "$VERSION" =~ -(beta|rc|alpha) ]]; then
              echo "prerelease=true"
            else
              echo "prerelease=false"
            fi
          } >> "$GITHUB_OUTPUT"

      - name: Stage release artifact
        env:
          NOTES_FILE: ${{ steps.notes.outputs.file }}
        run: |
          set -euo pipefail
          BUNDLE="$RUNNER_TEMP/release-bundle"
          mkdir -p "$BUNDLE"
          cp dist/*.dmg "$BUNDLE/"
          if compgen -G "dist/showcase/*.mp4" > /dev/null; then
            cp dist/showcase/*.mp4 "$BUNDLE/"
          fi
          cp "$NOTES_FILE" "$BUNDLE/release-notes.md"
          (
            cd "$BUNDLE"
            shasum -a 256 ./*.dmg ./release-notes.md > SHA256SUMS
            if compgen -G "*.mp4" > /dev/null; then
              shasum -a 256 ./*.mp4 >> SHA256SUMS
            fi
          )

      - name: Upload verified release artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: release-${{ env.RELEASE_TAG }}
          path: ${{ runner.temp }}/release-bundle
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  publish:
    name: Publish immutable release
    needs: release
    if: >-
      needs.release.result == 'success' &&
      github.repository == 'PathGao/kururu' &&
      ((github.event_name == 'push' && github.ref_type == 'tag') ||
       (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) &&
      github.actor == 'PathGao' &&
      github.triggering_actor == 'PathGao'
    runs-on: macos-26
    timeout-minutes: 10
    permissions:
      contents: write # Publishes the release; this job never receives Apple credentials.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Download verified release artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-${{ env.RELEASE_TAG }}
          path: release-bundle

      - name: Verify release artifact
        run: |
          set -euo pipefail
          cd release-bundle
          shasum -a 256 -c SHA256SUMS

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          COMMIT_SHA: ${{ needs.release.outputs.commit_sha }}
          NOTES_FILE: release-bundle/release-notes.md
          PRERELEASE: ${{ needs.release.outputs.prerelease }}
        run: |
          set -euo pipefail
          if [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]]; then
            # GITHUB_TOKEN does not trigger another tag workflow. The tag is only
            # created after the signed and notarized artifacts have passed verification.
            if ! gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_TAG" >/dev/null 2>&1; then
              gh api --method POST "repos/$GITHUB_REPOSITORY/git/refs" \
                -f "ref=refs/tags/$RELEASE_TAG" -f "sha=$COMMIT_SHA" >/dev/null
            fi
          fi
          REMOTE_COMMIT="$(gh api "repos/$GITHUB_REPOSITORY/commits/$RELEASE_TAG" --jq '.sha')"
          if [[ "$REMOTE_COMMIT" != "$COMMIT_SHA" ]]; then
            echo "Release tag does not match the verified source" >&2
            exit 1
          fi
          FILES=(release-bundle/*.dmg)
          if compgen -G "release-bundle/*.mp4" > /dev/null; then
            FILES+=(release-bundle/*.mp4)
          fi

          verify_release() {
            local expected_draft="$1"
            local release_id asset_count file name digest record
            local expected_metadata metadata body_base64 expected_body_base64

            release_id="$(gh release view "$RELEASE_TAG" --json databaseId --jq '.databaseId')"
            asset_count="$(gh api "repos/$GITHUB_REPOSITORY/releases/$release_id/assets" --jq 'length')"
            if [[ "$asset_count" -ne "${#FILES[@]}" ]]; then
              echo "Release contains unexpected assets" >&2
              return 1
            fi
            for file in "${FILES[@]}"; do
              name="$(basename "$file")"
              digest="sha256:$(shasum -a 256 "$file" | awk '{print $1}')"
              record="$(gh api "repos/$GITHUB_REPOSITORY/releases/$release_id/assets" \
                --jq '.[] | [.name, .digest, .uploader.login] | @tsv' \
                | awk -F '\t' -v name="$name" '$1 == name { print $2 "\t" $3 }')"
              if [[ "$record" != "$digest"$'\t'"github-actions[bot]" ]]; then
                echo "Release asset verification failed for $name" >&2
                return 1
              fi
            done

            expected_metadata="$RELEASE_TAG"$'\t'"$expected_draft"$'\t'"$PRERELEASE"$'\t'"github-actions[bot]"
            metadata="$(gh release view "$RELEASE_TAG" --json name,isDraft,isPrerelease,author \
              --jq '[.name, .isDraft, .isPrerelease, .author.login] | @tsv')"
            body_base64="$(gh release view "$RELEASE_TAG" --json body --jq '.body | @base64')"
            expected_body_base64="$(base64 < "$NOTES_FILE" | tr -d '\n')"
            if [[ "$metadata" != "$expected_metadata" || "$body_base64" != "$expected_body_base64" ]]; then
              echo "Release metadata verification failed" >&2
              return 1
            fi
          }

          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            IS_DRAFT="$(gh release view "$RELEASE_TAG" --json isDraft --jq '.isDraft')"
            IS_IMMUTABLE="$(gh release view "$RELEASE_TAG" --json isImmutable --jq '.isImmutable')"
            AUTHOR="$(gh release view "$RELEASE_TAG" --json author --jq '.author.login')"
            if [[ "$IS_DRAFT" == "false" ]]; then
              if [[ "$IS_IMMUTABLE" != "true" || "$AUTHOR" != "github-actions[bot]" ]]; then
                echo "Refusing to reuse an untrusted or mutable published release" >&2
                exit 1
              fi
              verify_release false
              echo "Release is already published, immutable and verified."
              exit 0
            fi
            if [[ "$AUTHOR" != "github-actions[bot]" ]]; then
              echo "Refusing to reuse an untrusted or mutable published release" >&2
              exit 1
            fi
            gh release upload "$RELEASE_TAG" "${FILES[@]}" --clobber
          else
            CREATE_ARGS=(
              "$RELEASE_TAG"
              "${FILES[@]}"
              --verify-tag
              --title "$RELEASE_TAG"
              --notes-file "$NOTES_FILE"
              --draft
            )
            if [[ "$PRERELEASE" == "true" ]]; then
              CREATE_ARGS+=(--prerelease)
            fi
            gh release create "${CREATE_ARGS[@]}"
          fi

          EDIT_ARGS=(
            "$RELEASE_TAG"
            --draft
            --title "$RELEASE_TAG"
            --notes-file "$NOTES_FILE"
          )
          if [[ "$PRERELEASE" == "true" ]]; then
            EDIT_ARGS+=(--prerelease)
          else
            EDIT_ARGS+=(--prerelease=false)
          fi
          gh release edit "${EDIT_ARGS[@]}"
          verify_release true

          if [[ "$PRERELEASE" == "true" ]]; then
            gh release edit "$RELEASE_TAG" --draft=false --prerelease --latest=false
          else
            gh release edit "$RELEASE_TAG" --draft=false --prerelease=false --latest
          fi
          IS_IMMUTABLE=false
          for _ in {1..10}; do
            IS_IMMUTABLE="$(gh release view "$RELEASE_TAG" --json isImmutable --jq '.isImmutable')"
            [[ "$IS_IMMUTABLE" == "true" ]] && break
            sleep 1
          done
          if [[ "$IS_IMMUTABLE" != "true" ]]; then
            echo "Published release is not immutable" >&2
            exit 1
          fi
          verify_release false
