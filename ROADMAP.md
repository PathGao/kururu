# kururu Roadmap

> 更新：2026-09-10
> kururu 是 [Vorssaint](https://github.com/vorssaint/vorssaint-utils) 的分叉（GPL-3.0-or-later）。界面名称、章鱼图标与独立应用身份已落地。正式签名、公证、自更新和旧资料迁移仍待后续处理。
> 当前代码入口为本仓库。设计与历史资料见 [kururu-note](../kururu-note/README.md)，当轮核查与待办分工见 [当前待办](../kururu-note/当前待办.md)。旧设计是参考，不自动恢复为需求。

---

## Done

第65轮执行状态（2026-09-10，21:18候选版本）：功能库计数/后台行为可读性及AX摘要已完成浅深实测；主面板左导航按用户要求仅图标，完整悬停/AX名称与右侧标题保留，zh/en/de/fr/es/ja六语已验（中文5分类，其余实用工具分类）。卸载选择拒绝反馈保留原保护门槛，31项测试与de/fr保护应用反馈已核；两宿主非破坏性选择/扫描/取消流程通过。完整回归12,367项、编辑器14项和偏好清理通过，早于最后12处本地化文案/三处UI操作按钮去重；最终21:18构建成功，七套自检0失败。最终设置宿主唯一选择按钮已验，紧凑宿主最后去重仅源码复核。用户随后确认这次等待也是系统权限尚未放行，并已完成授权；不列为扫描性能故障。授权后的同一恢复路径尚待重跑，不能把授权本身当成扫描完成证据。上述是执行进度，不扩大产品范围。

当前基线：`main` 的 `09bd9a0`，已包含视觉与历史趋势整合提交 `2d3d235`。下列历史数量来自早期分类重构，不代表当前文件或功能总数。

2026-09-09 核查：窗口行为、键盘、剪贴板合页及全局入口分组均已实现。软件包列表与受限升级、环境诊断、英文默认回落、章鱼视觉、CPU 核心示意与最近五分钟内存历史也已进入主分支，无需重复开发。

### 分类统一

当前功能目录与设置侧栏共享分类来源（`FeatureCatalog.swift`）：

```
全局入口      径向菜单 / 命令栏（快捷键在侧栏独立入口）
系统监控      CPU / GPU / 内存 / 网络 / 磁盘 / 电源 / 风扇控制
专注与节能    保持唤醒 / 亮度 / 蓝牙睡眠 / 清洁模式
窗口与桌面    窗口切换器 / Dock / 窗口行为
输入设备      鼠标 / 触控板 / 键盘
剪贴板与文件  剪贴板 / 访达快捷键 / 置物架 / 草稿板
捕获与内容处理
声音与设备    音量混音器 / 麦克风静音 / 阻止 Music
App 管理      清理器 / 卸载器 / 软件包 / 环境
```

功能、菜单栏图标、菜单栏面板等横切入口另列。窗口布局与快捷面板已删除。

### 名字只有一个来源

```
FeatureGroup.title      组名
AppFeature.name         功能名
SettingsPage.title      每个页的标题
```

六类消费者（功能页、侧栏、快捷键页、引导页、命令栏、菜单面板）全走这三个入口。
以前有 98 处直接读字符串，现在是 0，护栏挡着，允许清单是空的。
单功能页的标题就是那个功能的名字，页不再有自己的名字。

### 可安装的单位从「功能」改成「页」

早期按单功能装卸，后来改为按页启用。当前启用单位以 `FeatureUnit` 为准，成员开关以 `AppFeature.pageSwitchKey` 为准；六类普通指标没有独立成员总开关。当前历史模式在监控页启用后持续采集六类数据，隐藏图形只改变显示，关闭监控页才停止采样；风扇写控制保持单独开关。不能继续套用“每个成员必须有总开关”的旧规则。
开关关掉 = 以前的卸载：服务不启动，事件监听不装，定时器不跑。开销一分不多。

### 拆页

通用页解散，快捷面板解散，切换器拆出 Dock，能源拆四页，声音拆三页，鼠标拆出触控板。

### 字符串

6 个功能的文案从 953 字段的单体搬进各自的结构体；73 个没有任何读取者的字段删掉（每个都白翻译了 13 种语言）。

### 简体术语

```
三指中键        原「中键点按」——以后可能追加别的三指行为
窗口切换器      原「App 切换器」——它也在同一个 App 的多个窗口间切换
Dock            保留英文，和 App 同一条规则
```

### 迁移

老用户的可用性数据、拆开的预览设置键、旧版本的备份导入，都有一次性迁移。

---

## Trying

| 事项 | 状态 |
|---|---|
| **应用改名为 kururu** | 2026-09-10 已接独立身份：正式 `com.pathgao.kururu`，开发 `com.pathgao.kururu.dev`；包名、可执行文件、权限说明、helper、DMG 与项目链接共用产品定义。新版章鱼图标按assets/mascots/octopus原SVG适配，菜单栏使用同源单色轮廓，保留上游署名和来源。设置通过现有文件导入入口显式转移，登录项与系统恢复账本不导入，旧资料不自动移动；便条已接JSON/UTF-8文本预览选择导入，默认追加，明确勾选才替换唯一空白。剪贴板已接JSON与相邻图片目录导入、选择预览、后台准备和版本复核提交；旧偏好plist中的历史也已接入同一导入入口，图片通过另选目录读取，只提取历史字段；暂存架原子索引、写失败保护、退出保存及资产回收已接入，显式JSON/旧plist导入、整组选取、目录引用或复制、无覆盖附件事务与重启保真已接通。格式37项、资产27项、事务58项、实际导入服务55项及保存42项通过。第64轮实际验证选择/取消、整组预览、位置与模式双门槛、文件夹复制拒绝后修正重试、附件复制与目录映射、重启存储保真，生成样本已精确清理；架内取用和拖出仍待。没有自己的发布签名配置时，风扇助手与自更新明确不可用，临时分享关闭，反馈改成本地草稿。第64轮明确删除Beta更新渠道与独立支持页，项目/反馈集中关于，当前新功能采用产品六语说明。第51轮12,064项回归及14项原生编辑器检查通过，11:29开发构建和应用自检通过。新版章鱼保留八腕，菜单栏头部按用户反馈加宽加高；混音面板与设置共用全部/正在播放筛选，实际7项到1项及双向同步通过。零模块主面板已实测显示空态，首次引导和高级页可见品牌已修正。访达持续排列已实现，55项客户端及24项服务验证通过，真实读取失败的超时反馈已补，写入与重开保持待验。正式签名、公证、发布和完整资料迁移未完成。**`Sources/Vorssaint/` 内部目录名不改**。 |
| **灵动岛** | 顶部暂存与便条入口已在本地接通：章鱼、功能名称、数量与展开动作，设置可选菜单栏图标下方或屏幕顶部居中。便条复用同一原生面板与编辑器，顶部展示与暂存互相让位，可返回暂存架；不复制资料或重建编辑器。真实多屏拖放、输入法、焦点与原生撤销验收仍未完成。 |
| **径向菜单的媒体控制** | 2026-09-10核对当前源码：播放/暂停、上一首、下一首已通过媒体键执行，正在播放读取与详情入口也已接通。剩余是实际播放、切歌、详情和无媒体状态的体验验收，再决定保留范围；不能按早期撤回记录再次恢复代码。 |

---

## Planned

### 本地 AI 工作流与神经加速监测

2026-09-15 用户确定方向：支持已有本地模型、pipeline 附属工具、CLI 和 MCP，兼容自建环境与 Ollama。新增监测目标为独立 ANE 与 GPU 内部 Neural Accelerator 的占用率，**不新增 CPU、GPU、ANE 功耗**。下列均为规划或研究状态，尚未接入应用。

#### 占用率的接入条件与实测阻塞

| 编号 | 指标 | 2026-09-15 本机验证（M5，macOS 26.6.2） | 接入条件 |
|---|---|---|---|
| AI1 | 独立 ANE 计算占用率 | IOReport `PMP / Fast-Die CE / ANE0` 有 0%–100% 分档，但空闲及负载期间的分档增量均为零。使用已有 SpeakerKit 的 SpeakerEmbedder、合成输入、`cpuAndNeuralEngine` 配置，六秒完成 408 次预测；同步的五个一秒窗口内，ANE 带宽请求档位明显升高，计算分档仍无数据。 | 需要能在负载对照下产生有效增量的计算计数器。当前不能据此显示 0% 或计算占用率；配置允许 ANE 也不等于每个算子实际在 ANE 执行。 |
| AI2 | GPU 内部 Neural Accelerator 占用率 | Instruments 的 Performance Limiters 组可导出 `Neural Accelerator Utilization`。本机 GPUCounterGraph 将它映射到 `MXU Utilization`，并将 `MXU Limiter` 列为不同指标。公开 Metal counterSets 本次只返回时间戳，IOReport GPU Stats 未发现明确对应通道。 | 需要验证可持续、低开销的读取途径。普通进程的原始计数器插件初始化探测未成功。不能把 Instruments 能采集写成应用已能实时读取。 |

GPU 按需采集已验证能导出目标指标，但尚不满足产品接入要求：一次两秒 Performance Limiters 录制约 252 MiB，完整计数器 XML 导出为 1,878,412,007 字节。仅导出目标 row 的 XPath 尝试返回空结果，尚未得到可靠的小体积导出方式。这些是本次运行的成本，不是所有设备的固定成本。原始大文件为一次性探测产物，验证后清理。

**验收边界：** 不把功耗、带宽、电压/频率档位或带宽请求活跃时间标成计算占用率；不把无有效样本显示成零。读取必须有来源、时间窗口、缺失/失败状态，睡眠恢复重建基线，停止监控释放资源。GPU 的 Neural Accelerator 与独立 ANE 分开呈现。现阶段不添加只有不可用状态、没有已验证采集路径的仪表。

参考：[苹果 M5 GPU 性能工具演示](https://developer.apple.com/videos/play/tech-talks/111432/)、[aneperf 计算分档实现](https://github.com/tmc/aneperf)。外部实现中的电压或带宽档位回落不能用于上述计算占用率。

#### 本地环境的演进顺序

| 编号 | 阶段 | 范围 |
|---|---|---|
| AI3 | 资产与环境诊断 | 扩展现有环境页，按需检查已知位置及用户指定入口，关联模型文件、运行环境、CLI、附属工具与 pipeline。区分文件存在、可加载、运行中；未知入口不自动执行版本检查或试运行。Ollama 通过模型列表/运行中模型接口接入，自建 pipeline 通过显式登记入口与步骤接入。 |
| AI4 | 任务记录与性能对照 | 对用户登记并启动的任务记录阶段耗时、退出状态、内存和同期系统指标。先比较串行/并行、冷启动/热启动，再给优化建议。语言模型记录首字等待与生成速度，语音任务记录音频时长与处理耗时。不把整机计数器归因成单个进程独占。 |
| AI5 | CLI 与 MCP | 先诊断已有 CLI/MCP 的配置归属、实际路径、环境缺失和重复项。区分 stdio 按需进程与 HTTP 服务，不能按是否监听端口判定全部 MCP。连接检查可能启动程序，须由用户触发。Kururu 自身 CLI/MCP 先提供只读快照与诊断；执行管理和客户端配置写入单独设计。 |

首个验收案例为已存在的语音工作流：MLX Whisper（独立 Conda 环境）负责转写，Swift SpeakerKit/Core ML 负责说话人识别，ffmpeg 与后处理脚本负责前后处理。已安装的模型权重不因位于缓存目录就成为清理候选。该案例只提供验证对象，不把单机绝对路径写进产品实现。

目标是缩短任务耗时、控制内存压力、提高实际吞吐，不以所有硬件同时满载为成功标准。不重做通用包管理器、推理运行时或图形化 pipeline 编辑器。

### 对标软件

2026-09-09 确定以下方向。对标用于比较体验和取舍，功能范围仍以本路线图为准。

| 编号 | 模块 | 当前对标与定位 |
|---|---|---|
| B1 | 灵动岛 | 已核对 [Atoll 官方仓库](https://github.com/Ebullioscopic/Atoll) 与本地引用。Notchy有多个同名产品，原指代尚未确认。kururu顶部入口已接暂存与便条，共用原有面板并互相让位，真实输入法、拖放及跨屏仍待验收。 |
| B2 | 剪贴板 | **Deck、Frest**。09-10已核对Deck公开搜索、预览和逐项队列流程，身份指代与Frest准确来源仍待确认。kururu按找回、核对、复用审计，紧凑面板复制与删除已分离，真实交互待验。[对照与取舍](../kururu-note/设计参考/交互比较.md#剪贴板) |
| B3 | Dock | **DockDoor**，保留为体验细节参考。09-10核查点击/预览/退出链，修复只开点击时缺权限入口、退出请求失败隐式改为关窗口两处不一致。权限128项、退出33项通过，真实Dock与固定预览待验；不把旧版差距整批重列为待办。[公开流程与取舍](../kururu-note/设计参考/交互比较.md#窗口与dock) |
| B4 | 网络页端口管理 | **PortKiller**，沿用历史参考，仅涉及本地端口查询与占用进程操作。09-10复制按真实写入结果反馈，后台串行读写不阻塞主线程，6项实际方法检查通过，真实菜单待验。[公开流程与取舍](../kururu-note/设计参考/交互比较.md#本机端口) |
| B5 | 系统监控 | **Stats、iStat Menus**，指标呈现与菜单栏排布。当前监控行已补进程启动身份，确认后不重新认领复用PID，能源冲突快照不合并；实际方法15项与两种负对照通过。公开流程比较已记录，真实指标呈现与操作待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#监控与清理) |
| B6 | 命令栏 | **Raycast**，搜索与命令入口体验。09-10已核对主动作、操作分组与反馈边界，现有别名/快捷键/分类不重复开发；主动作与上下文提示共用执行决策，管理分组、滚动、复制/数值/路径失败、排序分区、App异步和系统设置结果均已接通。动作88项、路径32项及独立链15项、App入口31项、页脚实际方法126项与六语54图通过。真实键盘、输入法、窗口与焦点待验。[对照与取舍](../kururu-note/设计参考/交互比较.md#命令栏) |
| B7 | 窗口切换 | **AltTab**，切换交互与响应。09-10修复退出请求发出就提前移除窗口，现等待系统终止通知，会话结束撤观察者；15项实际方法与生命周期检查通过，真实保存确认、键盘与焦点待验。[公开流程与取舍](../kururu-note/设计参考/交互比较.md#窗口与dock) |
| B8 | 截图 | **CleanShot X、Shottr**，截图预览、编辑与捕获体验；快捷 OCR 继续参考 **TextSniper、Shottr**。09-10补复制/打开失败保留结果、OCR重试与跨入口回调归属、Trash失败不回退编号。23项实际方法和24张六语结果图通过，公开捕获与结果流程已核对，真实捕获与系统动作待验。[公开流程与取舍](../kururu-note/设计参考/交互比较.md#屏幕捕获) |
| B9 | 鼠标 | **Mac Mouse Fix、Mos、LinearMouse**，按键与滚动交互。当前Spaces按方向显示系统快捷键缺失，箭头随方向配置变化，227项和12张窄提示图通过；公开差异已记录，真实手势待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#鼠标) |
| B10 | 触控板 | **MiddleClick、Swish**，三指中键与手势体验。共享权限/设备/拖移阻断已覆盖径向单独启用，125项及缺陷变体通过；真实手势与完整页面待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#键盘与触控板) |
| B11 | 声音 | **SoundSource、PairPods、Tutti**，音频路由、多设备输出与延迟设置。当前输出切换区分无需动作、无可用选择和写入拒绝，命令栏具名反馈，11项方法检查通过；公开差异已记录，真实路由待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#声音与显示器) |
| B12 | 菜单栏管理 | **Pelmet（fif7y/pelmet）** 为 macOS 27 隐藏机制的优先研究对象；**Thaw** 持续跟踪，**Ice、Bartender、iBar Pro** 参考隐藏、展开与排序体验。适配范围与验收见下方「macOS 27 菜单栏」 |
| B13 | 显示器 | **BetterDisplay**，显示控制体验。当前亮度失败按屏提示并撤销未确认缓存，按请求及设备指纹隔离旧结果，20项与12张错误行图通过；公开差异及控制方式说明已记录，真实外屏待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#声音与显示器) |
| B14 | 键盘 | **Hyperkey**，组合修饰键交互。Super Key输入监听创建失败已补明确反馈及旧回调归属，9项实际生命周期与38项文案通过；公开差异已记录，真实改键待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#键盘与触控板) |
| B15 | 卸载与清理 | **AppCleaner、Pearcleaner**，卸载入口与残留扫描。执行中固定目标，Homebrew确认绑定包/路径/所选项目，普通删除防重入，10+13项隔离及12项真实Combine续清验证通过；公开差异与清理部分失败反馈已记录，实际界面待验；[公开流程与取舍](../kururu-note/设计参考/交互比较.md#监控与清理) |
| B16 | 文件暂存与处理 | 已核对**Yoink、Dropzone、Clop**官方公开流程，**Fasa**准确来源未定位。当前采用收集/交付与显式内容处理分工，暂存分享/移除分隔，六语前后48图通过；真实拖放和完整体验待验。[对照与取舍](../kururu-note/设计参考/交互比较.md#暂存架) |

**Maccy、Superpower 不再作为当前对标。** 历史记录保留，避免之后再次列入。
以上参考已按本轮确认纳入 roadmap，后续逐模块比较。对标不等于照搬全部功能，也不恢复已明确放弃的功能范围。其余历史名称与出处见 [历史对标索引](#历史对标索引)，仅存档的条目不自动进入计划。

### macOS 27 菜单栏

**计划适配，尚未实现。** 2026-09-09 调研：优先验证 [Pelmet（fif7y/pelmet）](https://github.com/fif7y/pelmet) 的系统原生隐藏机制，不以 Thaw 当前预发布版作为稳定依赖。区分 kururu 自身菜单栏图标的兼容性，与管理其他 App 图标的新增能力。当前 `StatusItemController.swift` 管理自己的主图标和多个监控图标，`MenuBarRenderer.swift` 已有 macOS 27 的绘制偏移处理，这不等于具备第三方图标管理能力。

- **M1 · 技术方向。** Pelmet 运行时加载苹果私有框架 `MenuBarClientCore`，通过 `MBAssessmentModeConfiguration` / `MBAssessmentModeAssertion` 提交允许显示的系统项目与 App bundle ID，由系统隐藏其余项目并重新排布。首轮只验证隐藏、展开、释放隐藏状态与能力不可用时的回退；macOS 26 及更早系统保留既有功能，不强行套用 27 的机制。优先复用系统原生动画，基础功能不增加屏幕录制权限。
- **M2 · 能力边界。** 该机制按 App 隐藏，同一 App 的多个图标需作为一组，不能承诺逐图标独立隐藏。kururu 自己的主图标与监控图标尤其要核验。Pelmet 文档说明：隐藏期间部分系统附加项（如 AirDrop、Focus、快速用户切换）会一并消失，时钟点击打开通知中心也受影响；时钟、控制中心、Siri 等受保护项目不能任意隐藏。先验证这些副作用是否可接受，不默认引入替代系统图标、模拟点击等补偿功能。
- **M3 · 排序单独评估。** Pelmet 的显隐与排序是两套路径。其 `AgentPositionStore.swift` 会写菜单栏代理的位置偏好，并在用户发起排序后终止代理，由系统重启，菜单栏会闪动。首轮以用户原生 ⌘-拖动排序为基础，不直接移植这条写系统偏好和重启进程的路径。多屏也不承诺独立布局：Pelmet 当前布局全局共享，仅按鼠标所在屏幕决定显隐行为。
- **M4 · 验收与失败恢复。** 记录 macOS 完整版本及 build，覆盖有/无刘海、单/多屏、全屏与 Spaces、菜单栏自动隐藏、睡眠唤醒、App 重启、权限拒绝/撤销，以及菜单栏项目动态增删。预期：隐藏后仍可可靠展开；退出、关闭功能或异常终止后系统项目恢复；私有类或方法缺失、激活失败时停止管理并报告不可用，kururu 其他功能继续工作。另测通知中心与系统状态指示、同 App 多图标、开发构建与 `/Applications` 安装构建的差异。Pelmet FAQ 报告 DerivedData 构建可能无法匹配系统的应用注册，导致白名单也无法保留其图标，需在 kururu 实机复现。
- **M5 · 参考版本与改判条件。** Pelmet 最新发布为 `v0.2.18`（2026-09-09），本轮源码基线 `fc202fd724d402c4b42dd0750f734d81410ac6df`。Thaw 于 2026-09-09 发布 `3.0.0-alpha.1`，要求 macOS 27 Beta 8+，仍走 Nightly；「尚不稳定」在此记录为用户体验与预发布状态，不当成已复现的具体缺陷。若后续 Thaw 在上述场景验证更可靠，或苹果提供公开接口，重新比较方案。直接复用 Pelmet 源码前保留并核对其 GPL-3.0 许可与版权声明。本轮未在 macOS 27 实机运行，不能据源码认定 Pelmet 比 Thaw 稳定。

调研依据（2026-09-09）：[Pelmet 私有接口桥接源码](https://github.com/fif7y/pelmet/blob/fc202fd724d402c4b42dd0750f734d81410ac6df/Packages/PelmetEngine/Sources/PelmetEngineObjC/MBAssessmentShim.m)、[排序与代理重启源码](https://github.com/fif7y/pelmet/blob/fc202fd724d402c4b42dd0750f734d81410ac6df/Packages/PelmetEngine/Sources/PelmetEngine/AgentPositionStore.swift)、[FAQ 与已知限制](https://github.com/fif7y/pelmet/blob/fc202fd724d402c4b42dd0750f734d81410ac6df/docs/FAQ.md)、[Thaw 3.0.0-alpha.1 发布说明](https://github.com/thaw-app/Thaw/releases/tag/3.0.0-alpha.1)。注意另有同名 `ismatBabirli/pelmet`，采用扩宽分隔符的旧机制，不是本条 macOS 27 方案的来源。

### 语言

**官方 6 种：英 / 简中 / 德 / 法 / 西 / 日。**

**回落机制已完成**（`refactor(l10n)`，净 −2210 行）。做法是把英文搬进声明当默认值：

```
以前   struct Strings { let menuQuit: String }      13 份字面量，每份必须填满
       static let enUS = Strings(menuQuit: "Quit Vorssaint", …)

现在   struct Strings { var menuQuit: String = "Quit Vorssaint" }
       static let enUS = Strings()                  英文就是声明本身
       static let de   = Strings(menuQuit: "Vorssaint beenden", …)   缺的自动读英文
```

44 个字符串结构体、2261 个字段都是这样。**加一个字段现在只写英文一处**，另外 12 份字面量不用动也照样编译。
调用点一个没改，编译时间没变（280 → 282 秒）。

**只有英文是必填的**，而且它必填是因为它就是那些默认值，写不漏。其余 12 种随意，翻多少算多少。

简体保留熟悉的英文原词（App、Dock）。唯一例外是「桌面与程序坞」——那是苹果系统设置面板的官方名字，用户要照着去点。

### 界面

所见即所得，优化视觉与动画。

**引导与可见套装入口已调整（2026-09-09，本地工作区）。** 功能页和引导中的套装入口已移除。引导按具体功能勾选，首次开启仅补必要基本动作，已有动作配置保留，同页未选功能保持关闭。普通监控合为一个入口，风扇单列并依赖监控。首次启动默认集合的内部代码仍保留。

**简体术语已完成（2026-09-09，本地工作区）。** 系统指标统一用「监控」，鼠标输入用「监听」；保留苹果 App 的正式名称「活动监视器」。功能可用性入口统一用「启用 / 关闭」，真正的 App / 软件包安装卸载名称保留。套装相关文案随可见入口一同移除。此处术语统一仅涉及简体；引导的行为变更与多语言说明见上一段。

**总开关与信息排布已完成首轮源码整理（2026-09-10，自动验证通过，UI待验收）。** 34 个设置路由已逐页静态审计。功能库管理整页可用性，页内只保留具体持续行为的开关。六个多行为组和四个按需捕获工具移除重复外层门禁，旧关闭状态迁移到具体动作或快捷键，参数与名单保留。开关放到所属选项旁，暂停后仍可准备配置。实际界面验收与缺口见 [交互审计](../kururu-note/验收记录/页面与流程.md)。

### 外观

2026-09-16 按用户要求移除 Liquid Glass 开关及专用渲染分支，移除 VS Code 主题导入、预览、持久化及配色覆盖。保留系统／浅色／深色选择和当前 baseline 的信息组织。旧备份里的两项外观字段按已退役设置忽略，不影响其他设置导入。首轮原生外观调整使用系统按钮与强调色，统一正文和小号开关比例；以中性底色、浅色分组卡片和细边界保留分区。菜单栏左侧导航改为固定圆角容器内滚动，溢出内容使用边缘渐隐。功能名称后保留权限图标，能耗与补充解释放入问号弹窗；部分设置说明改为按需帮助，保持唤醒的菜单栏外观选项可折叠。权限缺失、失败状态和操作后果继续直接显示。

### 监视

指标全给。「指标在菜单栏怎么显示」搬去菜单栏面板页，规则收成一句：**监视管数据，菜单栏面板管长相。**
结束进程砍掉独立页，能力并进监视——监视本来就在列谁吃 CPU/内存，右键结束是顺手的事。

**网络页已加入本机端口与占用进程（2026-09-09，本地工作区）**，具体边界见下方「端口」。

### 专注与节能

```
保持唤醒    留。卖点改成「电池模式下也能保持唤醒，还能定时」
            苹果只给「接电源 + 显示器关闭」，电池模式、定时、一键开关、合盖不睡都没有
亮度        一页收三样，都是「苹果给了一半」的那半：
            · 外接屏亮度   苹果键盘对外接屏无效
            · OSD 提示     调外接屏时苹果不给指示器，得自己画。不是装饰，是配套
            · 键盘背光快捷键  苹果只在有背光键的机型上覆盖了；新 MBP 的 F5/F6
                              变成了听写和勿扰，第三方键盘也没有，
                              那批人只能去控制中心拖滑块（上游 30cc597a 已实现）
            内置屏软件亮度入口已移除（2026-09-09）：面板、设置、命令栏及亮度键路由均保留系统原生控制；外屏亮度与显示器电源入口保留
蓝牙睡眠    留。苹果完全没有这个设置，是能拿出去说的差异点
清洁模式    留
```

### 窗口与桌面

```
窗口切换器    留，内部选项多，到时候逐条过
Dock          留（窗口预览 + 点按，已合成一页）
窗口行为      三个并成一页：
              · 绿灯改成最大化，而不是全屏
              · 关掉最后一个窗口时退出 App
              · ⌘Q / ⌘W 需要确认才生效
```

砍掉窗口布局之后这个组只剩三页：**切换器 · Dock · 窗口行为**。

「窗口行为」这个名字管的是**标准窗口控件按下去会发生什么**——交通灯和那两个快捷键。
它和 Dock 的最小化、切换窗口在认知上会有一点重叠，但 Dock 那页管的是「从 Dock 图标操作窗口」，
这页管的是「窗口自己的按钮和快捷键」，边界说得清。

### 输入设备 / 全局入口

分成两组：**输入设备**是鼠标、触控板、键盘（按键防抖 + 文本片段 + Super Key 三个成员合一页）；**全局入口**是键盘快捷键、径向菜单、命令栏——它们装的都是下面已启用的功能，自己没有内容，所以和「功能」「菜单栏图标」「菜单栏面板」一起排在最上面。
**命令栏是重点**——三个入口里它是唯一能搜索的。
快捷面板砍掉，能力并进菜单栏面板的「固定操作」区。

**径向菜单触控板入口已接入（2026-09-10，本地工作区）。** 每份配置可选择三指或四指轻点，抬手后菜单保持显示供点选，同配置再次轻点关闭。与中键共用既有触摸采集，暂停保留绑定，重复占用时不触发并显示冲突。52项识别、路由、配置、权限与备份检查及11,121项完整回归通过；真实触控板、系统手势竞争与窗口点选待解锁后验收。这条在上游提过 issue。

**触控板页是否合回鼠标仍待实际布局验收。** “只有三指中键”是早期判断。当前已支持三/四指中键，并显示径向轻点绑定与冲突，不能再以旧功能数量决定合页，也不为凑页面追加手势。

### 剪贴板与文件

剪贴板页收三样：历史、纯文本粘贴、清理 URL。体验对标 **Deck、Frest**，不再以 Maccy 为目标。
访达快捷键留（剪切粘贴 + 重命名）。
**新增：访达自动整理**——往文件夹里加文件图标不自动对齐，每次都要右键整理。注意文案：macOS 的「整理」是一次性对齐，「整理方式」才是持续按规则排，我们要的是后者的效果。
草稿板已统一为「便条」（2026-09-10，本地工作区）。复用已有浮动、置顶和自动保存能力，保留原有数据与偏好。顶部入口可打开同一个便条面板并返回暂存架；隐藏后恢复原浮动位置，切换展示位置保留编辑器与撤销。编辑器绑定刷新、销毁注销及跨便条撤销隔离已接入。普通保存失败显示状态与重试，改名/关闭失败保留对话框，正常退出前保存失败默认取消；57项实际服务检查通过，真实输入法、焦点与退出确认仍待验收。

### 捕获与内容处理

屏幕捕捉（截图 + OCR + 颜色吸管）、媒体（压缩视频图片 GIF）留。
**OCR 的打法：吃「快」，不吃「全」。** 一个快捷键，框一下，字进剪贴板，不弹窗不打断。对手是 TextSniper 和 Shottr 那一档，不是 CleanShot X。
**PDF合并与图片压缩已接入（2026-09-10，本地工作区）。** 合并支持多选或拖入PDF、页数、调序和移除；压缩接受单PDF，选择保持图片尺寸或适合屏幕，只有更小且校验通过才保存新文件，无收益正常提示且不生成副本。原件保留、已有目标不覆盖，取消和坏/加密文件有明确反馈。透明资源关闭JPEG优化，屏幕档仍可缩图；签名、表单、附件等无法验证保留的结构拒绝压缩。PDF专项与11,189项完整检查通过，合并与透明/批注压缩样本均完成原生渲染；第59轮已完成独立入口选择、调序、目录取消、合并输出与压缩结果实测；紧凑宿主及原媒体功能仍待完整验收。已有图片转PDF保留，其它转换继续排队。页面合并不承诺文档级书签、表单或数字签名保真。

**屏幕录制留，整个留。**（推翻了原先「只留 GIF、砍编辑器」的决定——那条的前提是「我们做的是半成品」，去读了代码之后发现不成立。）

这个品类真正值钱的活它已经在做了：`RecorderMotion` 656 行做的是点击时画面探进去、指针滑行而不抖，而且是离线算的——录完了才算，所以能同时看过去和未来，不像实时滤镜要拿延迟换平滑。再加上光标图库、背景和圆角、模糊区域、文字和图片叠加、打字轨迹。这些正是 Screen Studio 卖钱的东西。

砍掉的只有**临时链接**：把录屏传到服务器换一个 1 小时 / 6 小时的链接。那是别人替用户保管他的屏幕，归到下面第三类。

### 声音与设备

音量混音器（含输出切换）、麦克风静音、阻止 Music 都留。
**混音器是留得最理直气壮的一个**：Windows 2006 年就有，2026 年 macOS 还是只有一个总音量，苹果没有任何要做的迹象。

**App 启动拦截名单已实现（2026-09-09，本地工作区）。** 默认仅 Music，用户可添加、移除或清空名单，备份保留选择。首版复用现有媒体键触发机制：只拦截媒体键后两秒内的新启动，已运行 App 不受影响；该时间窗内的手动启动也可能命中。

**更广泛的启动拦截仍待决定。** 原设想还包括按其它键弹出的「词典」等情况，当前媒体键机制不能覆盖。不能把“名单可编辑”写成已能识别所有自动启动；扩展前需明确触发来源与手动打开豁免规则。

**麦克风结果与恢复已整理（2026-09-10，本地工作区）。** 区分静音请求、处理中、全部确认、部分失败、无设备与恢复失败，显示具体失败设备并提供重试。设置、菜单、命令栏与HUD使用同一结果，菜单栏不会保留过时的全静音标记。34项可运行设备与控制策略夹具及完整回归通过，真实设备和界面仍待验收。

新静音只使用可确认的硬件静音开关或主输入音量，无法安全恢复独立声道音量的设备明确失败。恢复保留原音量、用户后来设置的硬件静音，以及失败或离线设备的恢复记录。当前未增加录音、音频路由或会议自动化，输入设备与会议场景的进一步整合仍在后续队列。

### App 管理

清理、卸载器留。

**已实现：已装的软件包与受限升级。**

两条判据合起来划出边界：

```
包列表     列「你选择装的」，而且列给「看不见依赖关系」的人
清理器     列「自己长出来的」——没人主动装、没人知道存在、没人清
```

| 来源 | 怎么读 | 怎么分 |
|---|---|---|
| brew | `brew info --json=v2 --installed` | 每个包自带 `installed_on_request` 标记 |
| brew cask | `brew list --cask` | 全是主动装的 |
| mas | `mas list` | App Store，无依赖概念 |

brew 和 mas 是 Mac 上装东西的默认方式。brew 半技术用户也在用（照着教程粘一行就装了），
而**「哪些是依赖带进来的」正是他们看不见的东西**——列表里 `openssl@3` 有新版，
其实是 ffmpeg 拖进来的，跟着升容易炸环境。

**升级按钮只给主动装的那些。** 依赖不给 —— 它跟着需要它的包一起升，单独给按钮等于把这层关系藏起来。

**升级状态说明已补齐（2026-09-10，本地工作区）。** 主动安装且有更新的固定版本包显示行内原因，忙碌状态不误称版本固定，依赖继续只显示更新徽标。六种语言窄行布局已检查，真实点击与键盘待验收。

**已完成**（`feat(packages)`，净 −2344 行）：搜索和安装整个删掉，菜单栏面板那块也删掉；
卸载只从卸载器进（brew 装的 app 被卸载时要同步 brew 的记录）。

### 新增：全局环境（单开一页，只读）

放 App 管理下面自己一页。**不是列包，是让人看懂自己的环境长什么样。**

这一页的受众和包列表不同，也正是它的理由：普通用户不会打包管理器命令，
但会装 Cursor、粘一段 MCP 配置，然后卡在环境问题上——**他们被它坑，却完全看不见它。**

#### 四块内容

```
1  谁在赢
   node / npm / npx / python / bun 各自解析到哪个文件，版本多少
   同一个命令有几份，PATH 顺序决定了谁赢

2  哪些是 shim
   命令名骗人。显示：它是壳 → 真正执行的是谁 → 行为会有什么不同
   这是「npm 和 bun 混在一起」的根，比列包重要

3  GUI app 的 PATH ≠ 终端的 PATH
   并排显示两串，标出「终端有、GUI 没有」的那些
   一句话说清后果：从访达启动的 app 找不到这些命令
   ← 直接回答「MCP 配好了为什么起不来」，社区问一百遍的那个

4  缓存占了多少
   默认 npx / bunx 缓存，只报大小；清理动作在清理器
   不遍历项目 node_modules
```

再给一个**「复制诊断报告」**按钮——去 issue 区求助时贴这个，比来回问十轮快。

#### 本项目开发机上的实测（这就是要显示的东西）

```
node 有两份，版本还不一样
  /usr/local/bin/node        真 node   v24.16.0
  ~/.local/bin/node          shim→bun  报 v26.3.0     ← PATH 里赢的是这个

npm / npx 都是 shim
  npm  → 转给 bun 的脚本，注释写着 never falls back to Node
  npx  → 直接 exec bunx

launchctl getenv PATH 为空
  → 访达/程序坞启动的 app 拿到的是 launchd 默认 PATH
    ~/.local/bin、~/.bun/bin、/opt/homebrew/bin 都不在里面

PATH 里还有 /opt/anaconda3/condabin
bunx 缓存 2.3 GB
```

终端里 `node -v` 说 26.3.0（其实是 bun），真 node 24.16.0 躺在旁边没人用。
一个包说「需要 node 20+」，用户根本判断不了自己满不满足。

#### 能做的动作

**复制绝对路径**——这一页最有用的按钮。

```
npx    ~/.local/bin/npx        [复制]
node   ~/.local/bin/node       [复制]
bun    ~/.bun/bin/bun          [复制]
```

用户真正要解决的是「Claude Desktop 找不到 npx」。正解**不是**给全系统改 PATH，
是在那个 app 的配置里写绝对路径。一次点击解决社区问一百遍的问题，零风险。

**复制诊断报告**——去 issue 区求助时贴这个，比来回问十轮快。

**复制反馈已补齐（2026-09-10，本地工作区）。** 路径与诊断报告按系统写入结果显示成功或失败，刷新清除旧反馈，检查期间禁用复制。8项行为检查、独立剪贴板逐字读回及窄页离屏布局通过，真实点击和刷新时序待验收。

#### PATH 只能管一半，另一半不该假装能管

```
终端 PATH     能管。写 /etc/paths.d/cairn —— 我们自己的一个文件，一行一个路径。
              没有语法可错，删文件就撤销，要一次管理员授权。
              这是苹果给的正规位置，homebrew 自己就在用（/etc/paths.d/homebrew）。

              限制：path_helper 是把它追加在系统路径后面的，
              所以能让一个命令「被找到」，不能让某个 node「抢在别人前面」。

              绝不碰用户的 shell 配置。本项目开发机上 PATH 散在四个文件里改了 15 处
              （.zshenv 4、.zprofile 2、.zshrc 4、.bash_profile 5），
              我们再插一脚只会让以后排查更难。

GUI app PATH  改不了，也不该假装能改。
              launchctl setenv 要重启 app、重启电脑就没了、
              「登录时重开窗口」还会绕过它。
              这半靠上面那个「复制绝对路径」解决。
```

#### 不做

```
改 shell 配置文件         用户的 PATH 已经散在四个文件里，我们不进去搅
装卸 node / python 版本    那是 nvm / fnm / mise 的活
改全局 GUI PATH           平台没有可靠做法，做了是给用户埋雷
```

**已完成**（`feat(environment)`，+1057 行）：四块内容加两个复制按钮都在，落在 App 管理下自己一页。
终端 PATH 由用户自己的登录 shell 答（只有 shell 答得了），不读也不写他的配置文件；
不常驻，打开这页才查一次，每条命令都带超时。

**未实现且不属于本轮：写 `/etc/paths.d/cairn`。** 它要一次管理员授权，而且只能让命令「被找到」、不能让它抢在前面，
和这页的「读」是两件事，单独做。

#### 为什么这页取代了「列 npm 包」

```
列 npm 包    只有开发者装的东西，而开发者有终端        受众错了
全局环境      普通用户被它坑，但完全看不见它            受众对了
```

#### node 的东西全部归清理器，不进包列表

**缓存发现已补齐（2026-09-09，本地工作区）。** 清理器按需发现默认位置 `~/.npm/_npx` 和 `~/.bun/install/cache`，默认不勾选，沿用废纸篓与删除前身份复核。拒绝符号链接根或父路径，不遍历项目 `node_modules`，不改动用户配置的其它缓存位置。

一度想列 npm 全局包，理由是「普通 AI 用户在用 npm」。这句话不成立：

```
会跑 npm i -g 的人      在终端里打字的人——和 pip、cargo 同一批，
                        他们有终端，不需要我们代劳

普通 AI 用户干的事      装 Claude Desktop / Cursor，粘一段 MCP 配置，
                        里面是 npx -y @some/server
                        从头到尾没打过任何包管理器命令
```

**`npx` 不做全局安装**，东西落进缓存，全局目录里什么都没有。
所以「列 npm 全局包」列出来的恰恰只有开发者装的那些，正中「用的人自己就有终端」这一条。

普通用户真正产生的是**缓存**——那不是「已装的包」，是垃圾，归清理器：

```
~/.npm/_npx/              npx 缓存
~/.bun/install/cache      bunx 缓存   ← 本项目开发机上 2.3 GB
node_modules 缓存          各处
```

清理器扫这些目录时有个坑要记着：**命令名会被 shim 掉，目录不会**（细节见上面的「全局环境」）。
任何靠调命令定位缓存的写法，在装了 shim 的机器上都会失败。

### 端口

**已并入监控面板的网络页（2026-09-09，本地工作区），不另开独立功能页。** 参考 PortKiller，把「谁占着这个端口」和网络状态放在一起。本地 AI 等应用也会开启 localhost 服务，不只服务开发者。

- **P1 · 展示。** 列出本地监听端口、协议、监听地址、进程名与 PID，支持按端口或进程查找、复制信息。明确这是本机监听端口，不代表公网可达。
- **P2 · 操作。** 复用已有的结束进程能力，操作前明确目标进程。命令栏输入端口号仍可快捷查询，与网络页共享查询结果和操作逻辑。
- **P3 · 开销与权限。** 打开端口区域或手动刷新时按需查询，不加入监视指标的 1–5 秒后台采样。首版不提权，实际可见范围在实现时核验；读不到的进程信息标为受限，不把部分结果显示成完整结果。

本轮实现 TCP 监听与 UDP 绑定端口查询。网络页默认折叠，展开或手动刷新才查询；命令栏输入数字端口或 `port 8080` 共享结果。终止操作先确认，再复查端口与原始行绑定的进程启动身份，仅发送普通终止信号，不提权。端口转发、SSH 隧道仍不做。 第65轮已在实际主面板验证展开、过滤、无匹配、刷新、清空恢复、收起重开和行菜单查看取消；未执行复制或结束进程，破坏性流程不记通过。

---

## Not Do

这一节是判据，不是清单。**一个功能被砍，通常是因为它属于下面某一类，而不是因为它单独不好。**

### 一、苹果已经做了

做了等于用户装两份同样的东西，还得记住哪个在生效。

```
内置屏亮度            苹果键盘上那两个键就是干这个的
防止睡眠（接电源）     系统设置 → 电池 → 选项里有
窗口二分 / 四分        macOS 26 自带
```

注意边界：苹果做了**一部分**不等于整件事都被覆盖。保持唤醒留下来，是因为电池模式、定时、合盖不睡苹果都没给。判断的时候要问「苹果覆盖的是哪一半」。

### 二、这是一整个独立软件的品类

菜单栏工具做不好，也不该做。

```

Markdown 编辑器   草稿板往这个方向长就是另一个产品。（我自己有 Markpad）

窗口管理          27 种摆法是 Rectangle / Magnet 的赛道，它们免费且更成熟。
                  笔记本上窗口空间本来就不大，真正用得上的是超大屏用户，
                  而那批人已经在用专门的工具。
```



### 三、需要私有 API、长驻后台进程、或保管用户凭据

新增能力默认遵循这三条。上方明确列入研究的 macOS 27 菜单栏，以及本地 AI 神经加速计数器的只读探测，是私有 API 的有界研究例外，必须先通过各节的恢复、开销与平台验收，不能把研究方向写成已上线能力。现有依赖私有接口的功能另按可用性与风险审计，不据此整批移除。

```
私有 API      靠没文档的行为撑功能，系统一升级就碎，而且我们无法保证
长驻后台      为一个功能常驻一个进程，所有用户替这个功能付电和内存
托管凭据      用户的云盘密钥、SSH 密钥交给我们保管，一旦出事没法收场
```

被这条挡住的：把 Dock 锁在某个显示器（无 API）、罗技鼠标专属键（要私有 HID）、
端口转发和 SSH 隧道（长驻 + 凭据）、录屏上传到用户自己的云（凭据）。

端口查询能做，正是因为它查完就退，不常驻。

### 四、绕过了别人的安全机制

```
App 更新      各家 App 自己的更新通道有灰度分批。我们统一推，等于把那道拦截绕掉了。
              而且常用的 App 用户自己会更新，不常用的本来就不用更新。

Homebrew 依赖升级  brew 里一大半是依赖包（别的包拖进来的），用户不知道哪个是哪个。
                  在列表里看到 openssl@3 有新版就点升级，那是 ffmpeg 拖进来的，
                  升了容易炸环境。
```

这两条不是「功能不好」，是**我们的界面会诱导用户做错事**。App 更新整条去掉；
brew 这条**改成按 `installed_on_request` 分**：主动装的给升级按钮，依赖不给。
判据变了 —— 原来说「用户不知道哪个是哪个」，现在我们知道了，就告诉他。
残留的风险：`brew upgrade <包>` 仍会连带升它的依赖，日志里能看到。

### 五、它想省的那一步本来就不费劲

```
DMG 安装器    用户得先打开我们的安装器；拖拽式和安装包式事先分不清；
              也没法选装到哪。双击 dmg 拖一下再删掉，本来就不麻烦。
```

### 六、权限成本高过收益

```
相机预览      为了「开会前看看自己」，要用户把摄像头权限交给一个菜单栏工具。
              而开会软件自己就有预览。
```

### 七、同一件事已经有了入口

```
快捷面板      它那 27 个动作，命令栏一个不少，径向菜单也全放得下。
              它唯一独有的是「不用配置、点菜单栏就有」——而面板本来就要做
              「固定操作」区，那块就是它。
              三个入口各司其职：命令栏管搜索，径向菜单管鼠标，面板管常用。

结束进程      监视本来就在列谁吃 CPU/内存，右键结束是顺手的事，
              不需要一个独立的窗口和一页设置。
```

### 八、用的人自己就有终端

不以“开发者已有终端”单独排除功能。2026-09-15 本地 AI 方向允许整合跨工具、环境、模型和进程的诊断信息；仍不重复实现通用包管理器。下列排除范围针对安装、升级和通用包列表，不排除上述 AI 工作流的关联诊断。

```
Python 全家       pip、uv、conda、miniforge、venv……Python 的环境本来就是散的，
                  同一台机器上好几套并存。要列就得先列环境，列环境就要跨语言列，
                  摊子一下铺开。
                  而且但凡用到这些的基本都懂点开发——他们有终端，
                  不需要我们代为查看，更不需要我们代为升级。

npm / bun         作为「包管理器」都不列——会跑 `npm i -g` 的是终端用户。
                  普通 AI 用户走的是 `npx`，那不产生全局安装，只产生缓存，
                  而缓存归清理器。**列的是「东西在哪」，不是「你用什么工具」。**

cargo             Rust 开发者，同理。
                  （`cargo install --list` 能正确排除 rustup 工具链，
                  所以技术上不是问题，是受众问题。）

gem               机器上那份是系统自带的，动它有风险。
```

npm 全局包不列。普通 AI 用户产生的 npx / bunx 缓存由清理器发现，命令实际来源由全局环境页解释，避免旧讨论中的“npm 留下”被误读为恢复包管理入口。

### 九、测不了

```
超亮度        把亮度推到苹果标称值以上，本来就在标准外，偏色风险真实存在，
              而且我们没有设备验证。做一个自己没法验证的功能，等于把风险
              转嫁给用户。
```

---

## 待定

```
Q1  窗口布局砍掉后，上游那三条我放进队列的 issue 怎么处理（#1187 #1306 #1159）
Q2  HiDPI —— 先不做。缺口确实存在（非标准分辨率显示器苹果不给缩放选项），记着
Q3  灵动岛的顶部暂存与同编辑器便条已接通，真实交互与布局细节待验收
```


## 历史对标索引

> 核查：2026-09-09。当前采用与排除项以 [ROADMAP.md](#对标软件) 为准。

### 来源与适用范围

主要来源是 PathGao 发布的 [Related Apps Summary #880](https://github.com/vorssaint/vorssaint-utils/issues/880)。它汇总了社区提出的参考软件，不能把每个名字都解释成作者亲自选定的产品目标。此次同时检索了该账号在上游的 12 条自建 issue 和 1,342 条 issue / PR 会话评论。

原汇总以 Vorssaint 3.3.3、提交 `006ce953` 为基线。本索引只保留名字、比较领域与出处，旧文档中的缺失功能和优劣判断不能直接套到当前 kururu。涉及已放弃的窗口布局、应用更新器等领域，仅留历史记录。

本轮确认已将上一轮列出的模块参考纳入 roadmap，并明确加入 **Mac Mouse Fix**。Mac Mouse Fix 也出现在 #880 的次级名单中；本文件继续保留历史分组，当前对标状态统一查 roadmap。

### 主要历史参考

下表来自 #880 的 23 个主要条目，按领域合并。除明确标注外，均是原汇总收录的社区引用。

| 领域 | 软件 | 当时比较的内容 |
|---|---|---|
| 命令栏 | Raycast | 搜索、入口与命令交互 |
| 截图与录屏 | CleanShot X、Shottr | 截图预览、编辑和捕获体验 |
| 系统监控 | Stats、iStat Menus | 指标呈现、菜单栏排布 |
| 鼠标 | Mos、LinearMouse | 滚动、排除列表和按键行为 |
| 访达与系统操作 | Supercharge | 访达快捷键与系统开关 |
| 菜单栏管理 | Ice、Bartender、Thaw / iBar Pro | 隐藏分层、展开和排序 |
| 窗口切换 | AltTab | 切换交互和响应速度 |
| 声音 | SoundSource | 音频路由与设备操作 |
| Dock | DockDoor | 悬停预览、拖放和卡片呈现 |
| 窗口布局 | Rectangle / Rectangle Pro | 布局与快捷键，仅作历史记录 |
| 键盘 | Hyperkey | Caps Lock 组合修饰键 |
| 显示器 | BetterDisplay | 显示控制 |
| 剪贴板 | Maccy | 历史与粘贴体验，现已退出当前对标 |
| 触控板 | MiddleClick | 三指中键 |
| 卸载与清理 | AppCleaner、Pearcleaner | 卸载入口与残留扫描 |
| 更新检测 | Latest | 汇总作者主动补充，社区相关引用是 Mole / Updatest；不恢复更新器计划 |
| URL 清理 | ClearURLs | 汇总作者主动补充的规则模型参考 |

**名称区别：** 本轮用户停用的是「Superpower」，历史汇总写的是「Supercharge」。两者不擅自视为同一个名称，Supercharge 也不因此加入当前目标。

### 有进一步出处的参考

- **H1 · 手势：Swish。** [自定义手势 #967](https://github.com/vorssaint/vorssaint-utils/issues/967) 中出现，AltTab 同时作为触摸事件实现参考。
- **H2 · 多设备音频：PairPods、Tutti。** [多输出与独立延迟 #966](https://github.com/vorssaint/vorssaint-utils/issues/966) 分别涉及同时输出和每设备延迟。
- **H3 · 端口：PortKiller。** [范围取舍评论](https://github.com/vorssaint/vorssaint-utils/issues/880#issuecomment-5392800870) 区分端口查询与端口转发 / SSH 隧道；[命令栏覆盖 #977](https://github.com/vorssaint/vorssaint-utils/issues/977) 提过端口查询入口。当前入口以 roadmap 的网络页计划为准。
- **H4 · 截图翻译：MoePeek。** [翻译讨论中的归并记录](https://github.com/vorssaint/vorssaint-utils/issues/489#issuecomment-5404742486) 收录了报告者的参考，尚不表示采用。

现有 roadmap 另有 **TextSniper**（快捷 OCR）和 **Screen Studio**（录屏体验）。这是路线图已有内容，不冒充本次从 #880 新发现的条目。**Atoll、Notchy、Deck、Frest** 来自本轮明确指定，在上述历史检索中未发现同名记录。

### 其余历史名称

以下是 #880 的次级参考原名单，只作检索入口。动到相应领域时再读原 issue，不能仅凭软件名字推导功能需求。

<details>
<summary>展开次级参考名单</summary>

easy-move-resize, Swift Shift, Swish, AeroSpace, BetterTile, Tangrid, Better Stage, Glide, FloatyTool, AlwaysOnTop, PowerToys Always on Top, Space Rabbit, Blink, WhichSpace, Spaceman, InstantSpaceSwitcher, Wins, DockView 2, MarqueeText, Docky, Sol, Karabiner-Elements, Hammerspoon, KeyCue, KeyClu, Klack, KeyboardCleanTool, debounce-mac, Vimium, Mac Mouse Fix, BetterMouse, Logi Options+ / OpenLogi, Tutti, PairPods, AirPodsSanity, podsmute, AudioControlBar, eqMac, Boom, SoundSwitch (Windows), LightsOut, MonitorControl, AlDente, batt, Amphetamine, KeepingYouAwake, MacShot, PixPin, Snipaste, Snapzy, Yoink, Dropzone, Clop, Fasa, OpenInTerminal, RightMenu Master, Android File Transfer, Mole, Updatest, only-switch, Hand Mirror, Camera Preview, Antinote, ScreenScribe, LocalSend, Blip, Mouseless, Textream, SelfControl, PanicLock, TimeMachineEditor, Bluesnooze, WidgetScreen, PortKiller, Hidden File Cleaner, PDF Squeezer, PDFsam, BentoPDF, Onigiri, QuickConvert, noTunes, mactop, PeakHour, Tencent Lemon

</details>

### 不应转换成对标待办的提及

- **N1 · 替代软件建议。** 拒绝集成 MTP 时推荐的 [OpenMTP、LocalSend、calibre、MacDroid、Commander One](https://github.com/vorssaint/vorssaint-utils/issues/531#issuecomment-5409262178)，拒绝 Touch Bar 支持时推荐的 [Pock、MTMR](https://github.com/vorssaint/vorssaint-utils/issues/333#issuecomment-5409516268)，以及拒绝设备协议支持时推荐的 [OpenLogi](https://github.com/vorssaint/vorssaint-utils/issues/575#issuecomment-5409548641)，都不意味着计划重做这些功能。
- **N2 · 明确未采用的依赖。** [SoulverCore 讨论](https://github.com/vorssaint/vorssaint-utils/issues/947#issuecomment-5417052907) 是不采用的记录。
- **N3 · 兼容性报告。** #880 将 GeForce NOW、Parallels、Ghostty / iTerm2 / Warp、Deskflow / Barrier、Chromium 浏览器、BeyondTrust EPM、OBS、DaVinci Resolve 等另列为冲突对象，不是产品对标。Ice、Thaw、AppCleaner 等同时在其他上下文作为功能参考，需按具体引用区分。
